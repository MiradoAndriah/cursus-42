# A-Maze-ing – default configuration file
# Lines starting with # are comments and are ignored.

# Maze dimensions (number of cells)
WIDTH=20
HEIGHT=15

# Entry and exit coordinates: x,y  (col, row), starting at 0
ENTRY=0,0
EXIT=19,14

# Output file for the hex-encoded maze
OUTPUT_FILE=output_maze.txt

# Perfect maze = exactly one path between entry and exit
PERFECT=True

# Random seed for reproducibility (comment out for random each time)
SEED=42

# Algorithm: backtracker (default) or prim
ALGORITHM=backtracker
